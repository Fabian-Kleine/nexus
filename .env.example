# PostgreSQL (used by the app container)
DATABASE_URL=postgres://nexus:changeme@db:5432/nexus

# PostgreSQL container credentials
POSTGRES_DB=nexus
POSTGRES_USER=nexus
POSTGRES_PASSWORD=changeme

# SMTP / mailer
MAIL_HOST=smtp.example.com
MAIL_PORT=465
MAIL_USER=user@example.com
MAIL_PASS=changeme
